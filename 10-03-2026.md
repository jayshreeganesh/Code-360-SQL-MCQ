It looks like you've been busy crushing some DBMS quizzes on Code360! Below is the content of your screenshots converted into a clean, scannable Markdown format.

---

## 1. SQL Foreign Key (Purpose)

**Problem Statement:** What is the purpose of a Foreign Key in DBMS?

* **Options:**
* Uniqueness
* **Data integrity** (Correct)
* Data indexing
* Sort data



> **Solution description:** Foreign Key enforces referential integrity between tables.

---

## 2. Foreign Key (Linking)

**Problem Statement:** Which key links two tables in SQL?

* **Options:**
* Primary Key
* **Foreign Key** (Correct)
* Candidate Key
* Unique Key



> **Solution description:** A foreign key in one table references a primary key in another, establishing relationships between the two.

---

## 3. Attribute Types

**Problem Statement:** In SQL, the attribute `AGE` is calculated from `DATE_OF_BIRTH`. The attribute `AGE` is ......

* **Options:**
* Single valued
* Multi-valued
* Composite
* **Derived** (Correct)



> **Solution description:** The value for this type of attribute can be derived from the values of other related attributes or entities.

---

## 4. Update Ages (SQL Query)

**Problem Statement:** A University has the following dataset:

| Id | Name | Age |
| --- | --- | --- |
| 1 | Lawrence | 21 |
| 2 | Aniket | 18 |
| 3 | Carla | 25 |

The dataset was last updated 1 year ago, which means the age of every person in the dataset is 1 year older now. Choose the most appropriate option to Update the dataset accordingly to store the correct age of every person.

* **Options:**
* `UPDATE * in University Age=Age+1;`
* **`UPDATE University SET Age = Age+1;`** (Correct)
* `UPDATE University SET ALL Age=Age+1;`
* We cannot update all the rows at once.



> **Solution description:** In this question, we need to update the age of every person by one. So we can use the UPDATE query on the Database University and increase every element in the AGE column by one, hence we can do- `UPDATE University SET Age = AGE +1`.

---

## 5. ER Mapping

**Problem Statement:** Match the ER diagram components to their representation in the relational model.

| ER Component | Relational Model Representation |
| --- | --- |
| a. Entity | 1. Foreign key |
| b. Attribute | 2. Relation |
| c. Relationship between entities | 3. Columns |

* **Options:**
* a1 b2 c3
* **a2 b3 c1** (Correct)
* a2 b1 c3
* a3 b1 c2



> **Solution description:** An entity in ER diagram is represented by a relation or a table in relational model, similarly attributes of an entity are denoted by columns or fields of the table. Relationships between entities are shown by using foreign keys in relational models.

---

Would you like me to explain the logic behind any of these concepts, such as why a Foreign Key is specifically used for "referential integrity"?
