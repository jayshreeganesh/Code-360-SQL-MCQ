Based on the screenshots provided, here is the content from the **Code 360 by Coding Ninjas** MCQs converted into Markdown format.

---

## 1. Data Definition Language (DDL)
**Problem Statement:** Which is a DDL command in SQL?

* [ ] SELECT
* [ ] UPDATE
* [ ] DELETE
* [**X**] **CREATE**

**Solution Description:** CREATE is a DDL command used to create tables or databases.

---

## 2. Schema
**Problem Statement:** The state in a database management system that satisfies the constraints and specified schema structure is classified as:

* [**X**] **Valid state**
* [ ] Extension state
* [ ] Intention state
* [ ] Attribution state

**Solution Description:** *(Not explicitly visible in the screenshot, but marked as correct).*

---

## 3. Commit Rollback
**Problem Statement:** You can roll back after performing the commit in MySQL.

* [ ] True
* [**X**] **False**
* [ ] Depends on the Transaction

**Solution Description:** DBMS transactions have many queries, but there are no queries that will 'undo' the committed changes done to the transaction. Unless the user has a backup of a database, the user cannot Rollback after performing a Commit on SQL.

---

## 4. Sparse Indexing
**Problem Statement:** Which of the following is true?  
1. Sparse indices can be used only if the relation is stored in sorted order of the search key.  
2. It is generally faster to locate a record if we have a dense index rather than a sparse index.

* [ ] Only 1
* [ ] Only 2
* [ ] None
* [**X**] **Both 1 and 2**

**Solution Description:** Indexing is a method that helps us to locate the data present in the memory efficiently. There are two types of primary indexing: 1. Sparse indexing 2. Dense indexing. In the above question, the main focus is on the sparse indexing technique... It is generally faster to locate a record if we have a dense index rather than a sparse index. Sparse indices can be used only if the relation is stored in the sorted order of the search key.

---

## 5. Denormalization
**Problem Statement:** Designers use which of the following to tune the performance of systems to support time-critical operations in DBMS?

* [**X**] **Denormalization**
* [ ] Redundant optimization
* [ ] Optimization
* [ ] Realization

**Solution Description:** Designers use denormalization to tune the performance of systems to support time-critical operations in DBMS by reducing the number of joins and improving query performance.
