Here is the content from your screenshots converted into a structured Markdown format.

---

## 1. JOIN Operations

**Problem Statement:** What does an INNER JOIN return?

* **Options:**
* All records from both tables
* **Matching records from both tables** (Correct)
* Only unmatched records
* All records from one table


* **Solution Description:** INNER JOIN returns rows when there is a match in both tables.

---

## 2. DBMS Basics

**Problem Statement:** Which of the following is true:

* **Options:**
* Relation and table are same
* Attribute is column in table
* None of the above
* **A and B both** (Correct)


* **Solution Description:** For design purpose, table and relation means the same. Although, technically a relation does not have duplicate value but a table can contain duplicate. Similarly, the attributes and the column are the same.

---

## 3. Data Consistency Advantage

**Problem Statement:** Which of the following is a key advantage of DBMS over file systems regarding data consistency?

* **Options:**
* File systems use better indexing techniques
* **DBMS ensures consistent data by managing concurrent access** (Correct)
* File systems allow multiple users to access data simultaneously
* DBMS cannot handle concurrent data access


* **Solution Description:** DBMS is designed to handle concurrent data access, ensuring that users receive consistent data, whereas file systems may face issues with concurrent access and data consistency.

---

## 4. Query

**Problem Statement:** You are given a table `Employee` which has the following columns: `id`, `FirstName`, `LastName`. You have to use a query to display all those employees whose first names start with the letter 'R' and end with the letter 'I'. Some queries are given below:

(a) `SELECT * FROM Employee WHERE FirstName LIKE '%RI%';`
(b) `SELECT * FROM Employee WHERE FirstName LIKE 'R%' AND FirstName LIKE '%I';`
(c) `SELECT * FROM Employee WHERE FirstName LIKE 'R' AND 'I';`
(d) `SELECT * FROM Employee WHERE FirstName LIKE 'R%I';`

**Choose the most appropriate SQL query from below:**

* **Options:**
* Only (c)
* Only (b)
* **(b) and (d)** (Correct)
* (b), (c) and (d)
* (a) and (c)
* None of the above


* **Solution Description:** The `LIKE` operator is used to search for a specified pattern in a given column. The percent sign (%) wildcard, used in conjunction with the `LIKE` operator, represents zero, one, or multiple characters. Here, `LIKE 'R%'` finds values that start with 'R'. `LIKE '%I'` finds values that end with 'I'. `LIKE 'R%I'` finds values that start with 'R' and end with 'I'.

---

## 5. Storage Structure

**Problem Statement:** Which of the following model tells how data will be on the disk?

* **Options:**
* **Physical data model** (Correct)
* Creational data model
* Conceptual data model
* Representational data model


* **Solution Description:** Physical Data model represents the physical structure of the database which includes planning of how the data will be organised, stored on the disk and various access methods available for it.

---

Would you like me to explain any of these database concepts in more detail or provide more example queries?
