Based on the screenshots provided, here is the content of the quiz questions and answers formatted in Markdown.

---

## 1. Primary Key

**Problem Statement:**
What does a Primary Key enforce?

**Options:**

* Uniqueness and Null allowed
* **Uniqueness and Not Null** (Correct)
* Only Not Null
* Only Uniqueness

**Solution Description:**
The Primary Key ensures each record in the table is unique and cannot be NULL.

---

## 2. DBMS Functionality

**Problem Statement:**
Which of the following best describes the primary function of a Database Management System (DBMS)?

**Options:**

* To only store data
* **To ensure efficient data retrieval and management** (Correct)
* To convert data into flat files
* To enhance file system performance

**Solution Description:**
A DBMS acts as an interface between end-users and databases, helping in organizing, retrieving, and managing data effectively, which goes beyond just storing data.

---

## 3. Unary Operation

**Problem Statement:**
In the context of DBMS, which of the following is not a valid unary operation in relational algebra?

**Options:**

* Select
* **Min** (Correct)
* Project
* Rename

---

## 4. Type Constraint

**Problem Statement:**
In SQL, for an attribute "Adhaar Number" which is defined as integer type, which constraint would be violated if we enter the PAN (has numbers and alphabets) in it?

**Options:**

* Entity Integrity constraint
* Key constraint
* **Domain constraint** (Correct)
* Referential Integrity constraint

**Solution Description:**
The domain integrity constraints restrict the value in the particular attributes. It defines which values should be considered valid for a particular attribute and which values would be invalid. Therefore, if we want to take email as input in an attribute, then we can use domain constraints to ensure that email is valid. In this question, a similar situation is present as the Adhaar number is defined as an integer, which means the values entered in this attribute should contain digits only. On the other hand, the PAN number contains both digits and alphabets. This will result in the violation of domain integrity constraints and show Invalid status to the user.

---

## 5. DDL

**Problem Statement:**
The result of the Compilation of the DDL statement is stored in a special file called as __________.

**Options:**

* **Data Dictionary** (Correct)
* Symbol Table
* Database File
* Database schema File

---

Would you like me to explain the logic behind any of these specific DBMS concepts?
