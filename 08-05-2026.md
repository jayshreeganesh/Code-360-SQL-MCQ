Based on the screenshots provided, here is the content of the five Multiple Choice Questions (MCQs) from the "Problem of the Day" series on Code360, formatted in Markdown.

---

## 1. SQL NULL Handling

**Problem Statement:**
What is the `IS NULL` condition used for in SQL?

**Options:**

* [ ] Group data
* [x] **Check for NULL values**
* [ ] Sort data
* [ ] Join tables

**Solution Description:**
`IS NULL` checks if a value is NULL.

---

## 2. Views

**Problem Statement:**
SQL views are also known as ________.

**Options:**

* [ ] Simple tables
* [x] **Virtual tables**
* [ ] Complex tables
* [ ] Actual tables

**Solution Description:**
A view is also known as a virtual table because it contains rows and columns similar to a real table. It presents the table interface but cannot be stored as a physical table in the database.

---

## 3. Exclusive Mode

**Problem Statement:**
In DBMS, if a transaction has obtained a ________ lock, it can both read and write on the item.

**Options:**

* [ ] Shared mode
* [x] **Exclusive mode**
* [ ] Read only mode
* [ ] Write only mode

**Solution Description:**
If a transaction has obtained an exclusive mode lock, then it can both read and write on the item on which it is operating.

---

## 4. Schedules

**Problem Statement:**
In DBMS, the execution sequences in concurrency control are termed as ________.

**Options:**

* [ ] Serials
* [x] **Schedules**
* [ ] Organizations
* [ ] Timetables

**Solution Description:**
The execution sequences in concurrency control are known as schedules.

---

## 5. Trivial Dependency

**Problem Statement:**
What is trivial functional dependency in DBMS?

**Options:**

* [ ] If $A \rightarrow B$ holds where $B$ is not a subset of $A$.
* [x] **If $A \rightarrow B$ holds where $B$ is a subset of $A$.**
* [ ] If $A \rightarrow B$ holds where $B$ is not an intersection of $A$.
* [ ] If $A \rightarrow B$ holds where $B$ is an intersection of $A$.

**Solution Description:**
Let there be a set of attributes $A$ which determines a set of attributes $B$ (i.e., $A \rightarrow B$). Now if the dependent (i.e., $B$) is a subset of the determinant (i.e., $A$), i.e., If $A \rightarrow B$ and $B \subseteq A$, we call this a trivial functional dependency.
