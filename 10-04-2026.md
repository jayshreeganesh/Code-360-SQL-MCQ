Based on the screenshots provided, here is the content of the five DBMS-related multiple-choice questions from the **code360 by codingninjas** platform, converted into Markdown format.

---

## 1. SQL TRUNCATE Command
**Problem Statement:** What does the TRUNCATE command do?

* **Options:**
    * Deletes a table
    * **Removes all rows** (Correct)
    * Modifies table structure
    * Sorts data

**Solution Description:** TRUNCATE removes all rows from a table but retains the table structure.

---

## 2. Join condition
**Problem Statement:** Which is a join condition that contains an equality operator:

* **Options:**
    * **Equijoin** (Correct)
    * Cartesian
    * Natural
    * left

---

## 3. Relational Algebra
**Problem Statement:** In DBMS, Relational Algebra does not have

* **Options:**
    * Selection operator
    * Projection operator
    * **Aggregation operator** (Correct)
    * Division operator

**Solution Description:** Relational Algebra does not have an aggregator operator because it was designed as a set-based, foundational query language focused on basic operations like selection, projection, union, difference, and Cartesian product. Aggregation functions like SUM, COUNT, and AVERAGE are more complex operations that were introduced in SQL to meet practical needs, but they extend beyond the original minimalistic mathematical framework of Relational Algebra.

---

## 4. Database Definition
**Problem Statement:** Anything which stores only unstructured data can be called a Database.

* **Options:**
    * True
    * **False** (Correct)

**Solution Description:** It is not true as a database can contain both structured and unstructured data. Relational Databases contain only structured data whereas NoSQL (Eg: MongoDB) can contain unstructured data.

---

## 5. relationship 1
**Problem Statement:** In DBMS, a many-to-many relationship between two entities usually results in how many tables?

* **Options:**
    * ONE
    * TWO
    * **THREE** (Correct)
    * FOUR

**Solution Description:** A many to many relationships between two entities usually results in Three tables. *(Note: This typically includes the two entity tables and one junction/link table.)*
