It looks like you've been busy with some DBMS practice! Here is the content of your five screenshots converted into structured Markdown format.

---

## 1. SQL INSERT Statement
**Problem Statement:** What does the SQL `INSERT` statement do?

* **Options:**
    * [x] **Adds new data**
    * [ ] Deletes data
    * [ ] Updates data
    * [ ] Retrieves data
* **Solution Description:** `INSERT` adds new rows to a table.

---

## 2. ER Basics 1
**Problem Statement:** In the context of DBMS, the descriptive property possessed by each entity set is \_\_\_\_\_\_.

* **Options:**
    * [ ] Entity
    * [x] **Attribute**
    * [ ] Relation
    * [ ] Model
* **Solution Description:** The descriptive property possessed by each entity set is Attribute.


---

## 3. Data Abstraction Principles
**Problem Statement:** What is meant by data abstraction and independence in a DBMS?

* **Options:**
    * [x] **Data is separated from the application logic, allowing changes without affecting applications**
    * [ ] Data is physically stored in the same format as applications require
    * [ ] Application logic cannot be changed without modifying the data structure
    * [ ] Data is made accessible only to a specific group of users
* **Solution Description:** Data abstraction and independence ensure that the underlying data structure can be changed without impacting the application programs, offering flexibility and ease of maintenance.

---

## 4. Concurrent Access Explanation
**Problem Statement:** What does concurrent access in a DBMS allow?

* **Options:**
    * [ ] Only one user can access the database at a time
    * [x] **Multiple users can access the database simultaneously without conflicts**
    * [ ] Data is replicated across multiple databases for access
    * [ ] The database can only handle small amounts of data
* **Solution Description:** DBMS supports concurrent data access, allowing multiple users to interact with the database at the same time without interfering with each other's actions.

---

## 5. Type Constraint
**Problem Statement:** In SQL, for an attribute “Adhaar Number” which is defined as integer type, which constraint would be violated if we enter the PAN (has numbers and alphabets) in it?

* **Options:**
    * [ ] Entity Integrity constraint
    * [ ] Key constraint
    * [x] **Domain constraint**
    * [ ] Referential Integrity constraint
* **Solution Description:** The domain integrity constraints restrict the value in the particular attributes. It defines which values should be considered valid for a particular attribute and which values would be invalid. Therefore, if we want to take email as input in an attribute, then we can use domain constraints to ensure that email is valid. In this question, a similar situation is present as the Aadhar number is defined as an integer, which means the values entered in this attribute should contain digits only. On the other hand, the PAN number contains both digits and alphabets. This will result in the violation of domain integrity constraints and show Invalid status to the user.

---

Are you preparing for a specific certification or exam with these DBMS questions?
