Here are the contents of the five screenshots converted into Markdown format, organized by problem.

---

## 1. SQL DEFAULT Constraint
**Problem Statement:** What does the DEFAULT constraint do?

* **Selected Option:** Sets a default value
* **Other Options:**
    * Ensures unique values
    * Checks within a range
    * Prevents NULL values

**Solution description:**
DEFAULT sets a default value for a column when no value is specified.

---

## 2. Abstraction
**Problem Statement:** In the ER model, the details of the entities are hidden from the user. This process is called:

* **Selected Option:** Abstraction
* **Other Options:**
    * Generalization
    * Specialization
    * None of these above

**Solution description:**
In the ER model the details of the entities are hidden from the user. This process is called abstraction.

---

## 3. Entity Subgrouping
**Problem Statement:** The process of designating sub groupings within the entity set is called as ___________.

* **Selected Option:** Specialization
* **Other Options:**
    * Division
    * Aggregation
    * Finalization

**Solution description:**
With Respect to ER Model, specialisation is the procedure to split up the entities into further sub entities on the basis of their functionalities, specialties and features. These sub-designation of entities are distinctive from other entities in the set.

---

## 4. Delete & Truncate
**Problem Statement:** Which of the following statements is true in SQL?

* **Selected Option:** DELETE does not free the space containing the table and TRUNCATE free the space containing the table
* **Other Options:**
    * Both DELETE and TRUNCATE free the space containing the table
    * Both DELETE and TRUNCATE does not free the space containing the table
    * DELETE free the space containing the table and TRUNCATE does not free the space containing the table

**Solution description:**
The SQL TRUNCATE command is used to delete all the rows from the table and free the space containing the table.

---

## 5. Non Clustered Index
**Problem Statement:** How non clustered index point to the data?

* **Selected Option:** Points to rows with key values
* **Other Options:**
    * It never points to anything
    * It points to a data row
    * None of the mentioned

**Solution description:**
It is used for pointing data rows containing key values Nonclustered indexes have a structure separate from the data rows. A nonclustered index contains the nonclustered index key values and each key-value entry has a pointer to the data row that contains the key value.

---

Would you like me to explain the technical difference between any of these database concepts in more detail?
