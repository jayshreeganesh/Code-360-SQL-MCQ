It looks like you've been busy crushing some SQL and DBMS quizzes! Here is the content from your screenshots organized into a clean Markdown format for your notes.

---

## 1. SQL View
* **Problem Statement:** What is a view in SQL?
* **Options:**
    * A temporary table
    * **A virtual table** (Correct)
    * A data type
    * A physical table
* **Solution Description:** A view is a virtual table based on a SQL query.

---

## 2. Locking Protocol
* **Problem Statement:** In DBMS, the protocol that indicates when a transaction may lock and unlock each of the data items are called as \_\_\_\_\_\_\_\_.
* **Options:**
    * **Locking protocol** (Correct)
    * Unlocking protocol
    * Granting protocol
    * Conflict protocol
* **Solution Description:** The protocol that indicates when a transaction may lock and unlock each of the data items is called as locking protocol. Locking protocols restrict the number of schedules.

---

## 3. Normalization Step
* **Problem Statement:** Katherine was the assistant to the DBMS professor at her college. She was given a simple task of converting composite attributes into individual attributes for a table. What normalization is she performing in context to DBMS?
* **Options:**
    * **First** (Correct)
    * Second
    * Third
    * None of the above
* **Solution Description:** First Normal Form (1NF) is the first step of the normalization process. For a relation to justify 1NF, it needs to satisfy 4 basic conditions:
    1.  Each attribute should contain atomic values (i.e., no multivalued attributes).
    2.  Each value stored in an attribute should be of the same type.
    3.  All the attributes in a table should have unique names.
    4.  The order of the data stored in the table doesn't matter.
    In the given question, Katherine was given the task of converting composite attributes into individual attributes for a table. So it is in the First normal form.

---

## 4. Intersect Operator
* **Problem Statement:** What is true regarding the intersect operator?
* **Options:**
    * INTERSECT operation returns rows from the combined queries and NOT NULL values.
    * INTERSECT operation returns columns for the combined queries after eliminating duplicates.
    * **INTERSECT operation returns the common rows from the combined queries.** (Correct)
    * INTERSECT keyword is available in MySQL.
* **Solution Description:** INTERSECT operation returns only common rows in both queries after sorting them and removing duplicates.

---

## 5. Foreign Key
* **Problem Statement:** Which key links two tables in SQL?
* **Options:**
    * Primary Key
    * **Foreign Key** (Correct)
    * Candidate Key
    * Unique Key
* **Solution Description:** A foreign key in one table references a primary key in another, establishing relationships between the two.
