Below are the contents of the five screenshots from the **Code360 by Coding Ninjas** platform, formatted in Markdown.

---

## 1. SQL DELETE Statement
**Problem Statement:** What does the DELETE statement in SQL do?

* **Options:**
    * Retrieves data
    * **Removes data** (Correct)
    * Modifies data
    * Creates a table

**Solution Description:**
DELETE removes rows from a table.

---

## 2. Virtual Tables
**Problem Statement:** Which of the following statement is true in DBMS?

* **Options:**
    * Views protect sensitive data
    * Views compiled at runtime
    * Views improve query speed
    * **All of the Mentioned** (Correct)

---

## 3. Clustering Drawbacks
**Problem Statement:** Which of the following is not an advantage of database clustering?

* **Options:**
    * Load Balancing
    * Data Redundancy
    * Increased Availability
    * **Decreased Availability** (Correct)

**Solution Description:**
Clustering is the process of combining more than one server or instance holding the same database. We can say a cluster is a set of replicated servers. Clustering helps us with a lot of features like:
1.  **Data Redundancy:** Clustering databases helps with data redundancy, as we store the same data at multiple servers.
2.  **Load Balancing:** Clustering of databases also helps the servers with the load balancing i.e., if there's only one server and there are a lot of requests coming in to access the database then it may become difficult for a server to handle all the requests.
3.  **Increased Availability:** An availability of a database is defined as the time when we can access the database. Now as we have clusters of servers available, even if one of the databases is going through a transaction, now the other servers can be used to access the database with the help of a load balancer. Also, even if a server fails, the database will be available. Hence, due to clustering, the databases have high availability.

---

## 4. Clustered Index
**Problem Statement:** Which one is true about the clustered index?

* **Options:**
    * Not linked with table
    * **Built on unique key by default** (Correct)
    * Not built on unique key
    * None of the mentioned

**Solution Description:**
A clustered index is built by default on unique key columns. Nonclustered indexes have a structure separate from the data rows. A nonclustered index contains the nonclustered index key values and each key-value entry has a pointer to the data row that contains the key value.

---

## 5. Table Properties
**Problem Statement:** Which of the following is not a property of the table in DBMS?

* **Options:**
    * Every column should have unique name
    * **An attribute can have multiple values for a row** (Correct)
    * Column values should be of same type
    * Sequence of rows is insignificant

**Solution Description:**
A relation should contain atomic values, so multiple values for an attribute should be avoided.
