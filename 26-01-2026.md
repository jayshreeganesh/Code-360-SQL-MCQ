Here is the content from your screenshots organized into a clear Markdown format.

---

## **SQL Quiz: Problem of the Day**

### **1. SQL ALTER Command**

* **Question:** What does the `ALTER` command do in SQL?
* **Options:**
* Modifies data
* **Modifies table structure** (Correct)
* Deletes a table
* Joins tables


* **Solution Description:** The `ALTER` command is used to modify the structure of a table, such as adding a column.

---

### **2. Database Definition**

* **Question:** Anything which stores only unstructured data can be called a Database.
* **Options:**
* True
* **False** (Correct)


* **Solution Description:** It is not true as a database can contain both structured and unstructured data. Relational Databases contain only structured data whereas NoSQL (Eg: MongoDB) can contain unstructured data.

---

### **3. Subqueries-1**

* **Question:** Suppose a user has 3 queries Q1, Q2 and Q3. These queries are written in a way i.e. Q1(Q2(Q3)). Which SQL query will be executed first?
* **Options:**
* Q1
* Q2
* **Q3** (Correct)
* All are updated at the same time


* **Solution Description:** Subqueries is a way of using queries within a particular query. It is accomplished using the parenthesis. The inner query is always executed first, followed by the execution of the outer query. Inner query results in a single value or set of values as an output. In the above example, Q3 is the innermost query, so it should be executed first.

---

### **4. File System**

* **Question:** File System in which each teacher of a college is maintaining their separate files concerning the same subjects taught in the course would lead to:
* **Options:**
* High data integrity
* **High data redundancy** (Correct)
* High data security
* High efficiency to access data


* **Solution Description:** Since the given scenario will lead to presence of the same data at multiple locations, hence, it will lead to high redundancy.

---

### **5. Query (MCQ Problem)**

* **Problem Statement:** You are given a table `Employee` which has the following columns: `id`, `FirstName`, `LastName`. You have to use a query to display all those employees whose first names start with the letter 'R' and end with the letter 'l'.
* **Queries given:**
* (a) `SELECT * FROM Employee WHERE FirstName LIKE '%Rl%';`
* (b) `SELECT * FROM Employee WHERE FirstName LIKE 'R%' AND FirstName LIKE '%l';`
* (c) `SELECT * FROM Employee WHERE FirstName LIKE 'R' AND 'l';`
* (d) `SELECT * FROM Employee WHERE FirstName LIKE 'R%l';`


* **Options:**
* Only (c)
* Only (b)
* **(b) and (d)** (Correct)
* (b), (c) and (d)
* (a) and (c)
* None of the above


* **Solution Description:** The `LIKE` operator is used to search for a specified pattern in a given column. The percent sign (%) wildcard, used in conjunction with the LIKE operator, represents zero, one, or multiple characters.
* `LIKE 'R%'` finds values that start with 'R'.
* `LIKE '%l'` finds values that end with 'l'.
* `LIKE 'R%l'` finds values that start with "R" and end with "l". This query is equivalent to: `SELECT * FROM Employee WHERE FirstName LIKE 'R%' AND FirstName LIKE '%l';`



---

Would you like me to explain any of these SQL concepts, like the difference between `LIKE` patterns or how subqueries work, in more detail?
