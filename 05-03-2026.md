It looks like you've been working through a DBMS quiz on Code360. Here is the content of those screenshots converted into a clean, structured Markdown format.

---

## **1. SQL TRUNCATE Command**

**Problem Statement:** What does the TRUNCATE command do?

* [ ] Deletes a table
* [x] **Removes all rows**
* [ ] Modifies table structure
* [ ] Sorts data

**Solution Description:**
TRUNCATE removes all rows from a table but retains the table structure.

---

## **2. Database Schema**

**Problem Statement:** In DBMS, database schema is part of which design process:

* [ ] Conceptual design
* [x] **Logical design**
* [ ] Physical Design
* [ ] None of the above

**Solution Description:**
Going by the definition of database schema, it is the skeleton structure that demonstrates the logical view of the complete database. It is responsible for describing the organization of data and how the relations will be associated among them. It also formulates all the conditions that are to be applied on the data.

---

## **3. 2nf (Normal Forms)**

**Problem Statement:** Consider the schema $R(S, T, U, V)$ and the dependencies $S \rightarrow T, T \rightarrow U, U \rightarrow V, V \rightarrow S$ in DBMS. Let $R = \{R1, R2\}$ such that $R1 \cap R2 = \Phi$. Then the decomposition is:

* [ ] not in 2NF
* [ ] in 2NF but not in 3NF
* [ ] in 3NF but not in 2NF
* [x] **in both 2NF and 3NF**

**Solution Description:**
Then, the decomposition is in 2NF but not in 3NF.
*(Note: There is a discrepancy in the screenshot between the selected green option and the text in the solution description; usually, cyclic dependencies where every attribute is prime result in 3NF/BCNF).*

---

## **4. Table Properties**

**Problem Statement:** Which of the following is not a property of the table in DBMS?

* [ ] Every column should have unique name
* [x] **An attribute can have multiple values for a row**
* [ ] Column values should be of same type
* [ ] Sequence of rows is insignificant

**Solution Description:**
A relation should contain atomic values, so multiple values for an attribute should be avoided.

---

## **5. ER Basics 1**

**Problem Statement:** In the context of DBMS, the descriptive property possessed by each entity set is ________.

* [ ] Entity
* [x] **Attribute**
* [ ] Relation
* [ ] Model

**Solution Description:**
The descriptive property possessed by each entity set is Attribute.

---

Would you like me to explain the logic behind any of these specific DBMS concepts, such as why a schema is considered "Logical Design"?
