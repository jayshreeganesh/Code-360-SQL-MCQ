Based on the screenshots provided, here is the content of the five MCQ problems from the Code360 platform in Markdown format.

---

## 1. SQL Indexes

**Problem Statement:**
What is the purpose of an index?

* Sorts data
* **Speeds up queries** (Correct)
* Joins tables
* Group data

**Solution description:**
Indexes are used to speed up query processing.

---

## 2. Sparse Indexing

**Problem Statement:**
Which of the following is true?

1. Sparse indices can be used only if the relation is stored in sorted order of the search key.
2. It is generally faster to locate a record if we have a dense index rather than a sparse index.

* Only 1
* Only 2
* None
* **Both 1 and 2** (Correct)

**Solution description:**
Indexing is a method that helps us to locate the data present in the memory efficiently. There are two types of primary indexing: 1. Sparse indexing 2. Dense indexing. In the above question, the main focus is on the sparse indexing technique, a variety of index columns stores an equivalent block address. When data is retrieved, the block address is going to be fetched. That is why sparse indices are smaller in size in comparison to dense indices. That is why it is generally faster to locate a record if we have a dense index rather than a sparse index. Sparse indices can be used only if the relation is stored in the sorted order of the search key.

---

## 3. Null Meaning

**Problem Statement:**
In DBMS, a null value of an attribute indicates which of the following:

* Zero
* Infinite
* Error
* **Unknown** (Correct)

**Solution description:**
NULL value implies that the value for that particular attribute is unknown, this is not same as 0 or error.

---

## 4. Subqueries-1

**Problem Statement:**
Suppose a user has 3 queries Q1, Q2 and Q3. These queries are written in a way i.e. Q1(Q2(Q3)). Which SQL query will be executed first?

* Q1
* Q2
* **Q3** (Correct)
* All are updated at the same time

**Solution description:**
Subqueries is a way of using queries within a particular query. It is accomplished using the parenthesis. The inner query is always executed first, followed by the execution of the outer query. Inner query results in a single value or set of values as an output. In the above example, Q3 is the innermost query, so it should be executed first.

---

## 5. Procedure Lookup

**Problem Statement:**
Suppose you need to look out for the procedure containing the word "blog" (it can be anywhere in the name: at the beginning, in the middle or end of the name), which command should be used in SQL:

* `SHOW PROCEDURE STATUS WHERE db = 'blog';`
* `SHOW PROCEDURE STATUS LIKE '%blog';`
* `SHOW PROCEDURE STATUS WHERE db = '%blog%'`
* **`SHOW PROCEDURE STATUS LIKE '%blog%'`** (Correct)

---

Would you like me to explain any of these database concepts in more detail?
