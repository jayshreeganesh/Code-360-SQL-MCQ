Based on the screenshots provided, here is the content of the five database-related MCQ problems formatted in Markdown.

---

## 1. SQL Aggregate Functions
**Problem Statement:** What does the `COUNT` function do in SQL?

* **Options:**
    * [x] **Counts rows**
    * [ ] Counts columns
    * [ ] Adds values
    * [ ] Finds the max value

**Solution Description:** `COUNT` returns the number of rows in a query.

---

## 2. Transaction Control
**Problem Statement:** Suppose we have created a savepoint named `Spt1`, which of the following commands in SQL can be used to rollback the transactions executed after creating the savepoint:

* **Options:**
    * [ ] `RELEASE SAVEPOINT Spt1`
    * [ ] `COMMIT Spt1`
    * [x] **`ROLLBACK TO Spt1`**
    * [ ] `ROLLBACK`

**Solution Description:** **ROLLBACK transaction** - This TCL commands undo all the changes or restore the current state to any previously saved state. General form: `ROLLBACK`: This rollback the previously committed command to its initial state `ROLLBACK TO savepoint`: This undoes the current state and restores it to the savepoint.

---

## 3. Data Fragmentation
**Problem Statement:** Some of the columns of a relation are at different sites in which of the following techniques in SQL?

* **Options:**
    * [ ] Data Replication
    * [ ] Horizontal Partitioning
    * [x] **Vertical Partitioning**
    * [ ] Horizontal and Vertical Partitioning

**Solution Description:** To partition the data they are two techniques:
1.  **Vertical Partitioning:** In this, we partition the given data vertically i.e column-wise. So, if we are provided with table students with attributes student id, name, courseid, address, we can store this data by distributing it among servers where we store studentid, name is one server, courseid in another and address in the third one.
2.  **Horizontal Partitioning:** In this, we partition the given data horizontally i.e. row-wise. So, have chunks of a certain size of data stored at different servers.

---

## 4. Sparse Indexing
**Problem Statement:** Which of the following is true.
1. Sparse indices can be used only if the relation is stored in sorted order of the search key.
2. It is generally faster to locate a record if we have a dense index rather than a sparse index.

* **Options:**
    * [ ] Only 1
    * [ ] Only 2
    * [ ] None
    * [x] **Both 1 and 2**

**Solution Description:** Indexing is a method that helps us to locate the data present in the memory efficiently. There are two types of primary indexing: 1. Sparse indexing 2. Dense indexing. In the above question, the main focus is on the sparse indexing technique, a variety of index columns stores an equivalent block address. When data is retrieved, the block address is going to be fetched. That is why sparse indices are smaller in size in comparison to dense Indices. That is why it is generally faster to locate a record if we have a dense index rather than a sparse index. Sparse indices can be used only if the relation is stored in the sorted order of the search key.

---

## 5. Unary Operation
**Problem Statement:** Which is a unary operation in database:

* **Options:**
    * [ ] Selection operation
    * [ ] Primitive operation
    * [ ] Projection operation
    * [x] **Generalized selection**

**Solution Description:** Generalization Selection takes only one argument for operation.
