Here is the content from the uploaded screenshots transcribed into clean Markdown format.

---

## Question 1: SQL WHERE Clause

**Problem Statement:** What is the purpose of the WHERE clause in SQL?

**Options:**

* [ ] Order data
* [x] Filter data
* [ ] Join tables
* [ ] Group data

**Solution Description:** WHERE is used to filter records in a query.

---

## Question 2: Database Schema

**Problem Statement:** Which of the following language is used to specify database Schema in DBMS?

**Options:**

* [ ] Data Management Language
* [ ] Data Manipulation Language
* [ ] Data Development Language
* [x] Data Definition Language

---

## Question 3: Procedure Alteration

**Problem Statement:** Which of the following is a correct command if we have to alter an existing Stored Procedure in MySQL?

**Options:**

* [ ] ALTER PROCEDURE Sp1;
* [ ] ALTER PROCEDURE Sp1 ADD PARAMETER OUT w VARCHAR(10);
* [ ] ALTER PROCEDURE Sp1 ADD PARAMETER SET VALUE = DEFAULT;
* [x] Can't alter in mysql, we must drop the procedure and create a new one.

---

## Question 4: Transaction Phase

**Problem Statement:** In DBMS, a transaction that has not been completed successfully is called _______.

**Options:**

* [ ] Compensating transaction
* [x] Aborted transaction
* [ ] Active transaction
* [ ] Partially committed transaction

**Solution Description:** An aborted transaction is a state after the transaction has been rolled back and the database has been restored to the state prior to the transaction.

---

## Question 5: DBMS Architecture

**Problem Statement:** In the context of DBMS, if your laptop has an application like MS Access that is fetching data from another system, it is an example of which type of architecture?

**Options:**

* [ ] 1 tier
* [x] 2 tier
* [ ] 3 tier
* [ ] None of the above.

**Solution Description:** Since in a two-tier architecture, an interface runs on a client, and a data layer gets stored on a server. In the case of MS access also, an interface is running on your laptop which is actually fetching data from some database on another system. Hence it is an example of 2-tier architecture.
