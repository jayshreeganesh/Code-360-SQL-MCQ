Here is the content of the screenshots you uploaded, organized into a clear Markdown format.

---

## **Quiz: Database Management Systems (DBMS)**

### **1. SQL Aggregate Functions**

* **Question:** What does the `COUNT` function do in SQL?
* **Selected Answer:** **Counts rows**
* **Solution Description:** `COUNT` returns the number of rows in a query.

---

### **2. Select Operation**

* **Question:** For select operation the ________ appear in the subscript and the ________ argument appears in the parentheses after the sigma in SQL.
* **Selected Answer:** **Predicates, relation**
* **Technical Notation:** 

---

### **3. Composite Key**

* **Question:** In DBMS, a primary key is made up of two or more columns then it is called __ primary key.
* **Selected Answer:** **Composite**

---

### **4. Trivial Dependency**

* **Question:** What is trivial functional dependency in DBMS?
* **Selected Answer:** **If  holds where  is a subset of .**
* **Solution Description:** Let there be a set of attributes  which determines a set of attributes  (i.e., ). Now if the dependent (i.e., ) is a subset of the determinant (i.e., ), i.e. If  and . Hence, we call this a trivial functional dependency.
* **Visual Representation:** (Venn diagram showing circle  entirely contained within circle ).

---

### **5. Relational Algebra**

* **Question:** In DBMS, Relational Algebra does not have
* **Selected Answer:** **Aggregation operator**
* **Solution Description:** Relational Algebra does not have an aggregator operator because it was designed as a set-based, foundational query language focused on basic operations like selection, projection, union, difference, and Cartesian product. Aggregation functions like `SUM`, `COUNT`, and `AVERAGE` are more complex operations that were introduced in SQL to meet practical needs, but they extend beyond the original minimalistic mathematical framework of Relational Algebra.

---

**Would you like me to explain the logic behind any of these database concepts in more detail?**
