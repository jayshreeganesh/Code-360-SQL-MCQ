Here is the content of your screenshots converted into a structured Markdown format. It looks like you've been tackling a series of DBMS and SQL practice problems!

---

## 1. SQL NULL Handling

**Problem Statement:** What is the `IS NULL` condition used for in SQL?

| Option | Correct? |
| --- | --- |
| Group data |  |
| **Check for NULL values** | ✅ |
| Sort data |  |
| Join tables |  |

**Solution Description:**
`IS NULL` checks if a value is NULL.

---

## 2. Multivalue Attribute

**Problem Statement:** Which of these can be a multivalued attribute in DBMS?

| Option | Correct? |
| --- | --- |
| **Phone_number** | ✅ |
| Name |  |
| Date_of_birth |  |
| All of the mentioned |  |

**Solution Description:**
Name and Date_of_birth cannot hold more than 1 value.

---

## 3. ER Basics 2

**Problem Statement:** In DBMS, every weak entity set can be processed into a strong entity set by:

| Option | Correct? |
| --- | --- |
| using generalization |  |
| **adding appropriate attributes** | ✅ |
| using aggregation |  |
| none of the above |  |

**Solution Description:**
Every weak entity set can be converted into a strong entity set by adding appropriate attributes.

---

## 4. Constraint

**Problem Statement:** Which of the following is not a constraint in SQL?

| Option | Correct? |
| --- | --- |
| Primary Key |  |
| Not Null |  |
| Check |  |
| **Union** | ✅ |

**Solution Description:**
A constraint specifies the rules for allowing or restricting what data will be stored in a table. PRIMARY KEY, NOT NULL, and CHECK are constraints that define rules for data insertion. UNION is an operator that combines the results of two or more SELECT queries into a single result set.

---

## 5. Drawback of DBMS

**Problem Statement:** What is/are the drawback(s) of using DBMS?

| Option | Correct? |
| --- | --- |
| Requires training |  |
| Complex to use |  |
| It is costly |  |
| **All of the above** | ✅ |

**Solution Description:**
Using DBMS leads to the following disadvantages:

* It **requires training** because of the complexity associated with it.
* It is **complex to use** for non-technical users as it isn't easy to maintain and manage the database systems.
* It has a **very high cost** associated with it as it requires a high-speed processor and huge memory size to use the database on the DBMS.

---

Would you like me to explain the difference between a **Weak Entity** and a **Strong Entity** in more detail?
