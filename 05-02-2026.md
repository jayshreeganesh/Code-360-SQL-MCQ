It looks like you’ve successfully cleared a daily MCQ challenge on Code360! Here is the content from your screenshots organized into a clean Markdown format for your records or study notes.

---

## **1. SQL Foreign Key**

* **Problem Statement:** What is the purpose of a Foreign Key in DBMS?
* **Selected Answer:** **Data integrity**
* **Solution Description:** Foreign Key enforces referential integrity between tables.

---

## **2. Address Field**

* **Problem Statement:** In DBMS, the address field of a person should not be part of the primary key, since it is likely to?
* **Selected Answer:** **Changed**
* **Solution Description:** Address field of a person should not be part of primary key, since it is likely to Changed.

---

## **3. Type Constraint**

* **Problem Statement:** In SQL, for an attribute "Aadhaar Number" which is defined as integer type, which constraint would be violated if we enter the PAN (has numbers and alphabets) in it?
* **Selected Answer:** **Domain constraint**
* **Solution Description:** The domain integrity constraints restrict the value in the particular attributes. It defines which values should be considered valid for a particular attribute and which values would be invalid.

---

## **4. Graph Databases**

* **Problem Statement:** Out of the following, for which situation a graph based database will be apt?
* **Selected Answer:** **Semantic Search**
* **Solution Description:** Semantic search is search with meaning, as opposed to "normal" search where the search engine looks for literal matches... It understands the searcher's intent and the contextual meaning of terms in the Web. Hence, graph based database will be able to workout these relationships and come up with better results.

---

## **5. Data Export**

* **Problem Statement:** Choose the correct query for Exporting data in SQL, from the following.
* **Selected Answer:** ```sql
SELECT * FROM UFC WHERE id = 1 INTO OUTFILE 'C:/tmp/conner_vs_khabib2.csv'
FIELDS ENCLOSED BY '"' TERMINATED BY ',' ESCAPED BY '"' LINES TERMINATED BY '\r\n';
```

```


* **Solution Description:** To export our data into a CSV file.
1. **FIELDS ENCLOSED BY** is used when we have to export the data from the specific field.
2. **TERMINATED BY** is used when we have to terminate the data after export.
3. **LINES TERMINATED BY** is to terminate the specific line after exporting the data.



---

**Would you like me to explain the logic behind any of these specific SQL constraints or the graph database concept further?**
