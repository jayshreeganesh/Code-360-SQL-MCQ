Here is the content from the screenshots organized into Markdown format.

---

## **Quiz Summary: DBMS Concepts**

### **1. SQL Transactions**
* **Question:** What does a transaction in SQL do?
* **Options:**
    * Joins tables
    * **Ensures data consistency** (Correct)
    * Sorts data
    * Groups data
* **Solution Description:** A transaction ensures a sequence of SQL operations is completed successfully.

---

### **2. Selection Operation**
* **Question:** Which of the following is used to denote the selection operation in relational algebra in DBMS?
* **Options:**
    * Pi (Greek)
    * **Sigma (Greek)** (Correct)
    * Lambda (Greek)
    * Omega (Greek)
* **Solution Description:** The select operation selects tuples that satisfy a given predicate.

---

### **3. Database Schema**
* **Question:** In DBMS, database schema is part of which design process:
* **Options:**
    * Conceptual design
    * **Logical design** (Correct)
    * Physical Design
    * None of the above
* **Solution Description:** Going by the definition of database schema, it is the skeleton structure that demonstrates the logical view of the complete database. It is responsible for describing the organization of data and how the relations will be associated among them. It also formulates all the conditions that are to be applied on the data.

---

### **4. Key Concepts**
* **Question:** Which of the following is true:
* **Options:**
    * A candidate key is a minimal super key.
    * Candidate keys which do not become primary keys are called alternate keys.
    * Any candidate key can become a Primary key.
    * **All of the above** (Correct)
* **Solution Description:** A candidate key is a minimal subset of super keys. It contains no redundant attribute. Hence, it is selected from the set of super keys given that those selected keys DO NOT have any redundant attributes. Candidate Key value should not be null. Any candidate key can be selected to be a primary key according to preference. The candidate keys that are not selected as the primary key are called the alternate keys.

---

### **5. DDL Statements**
* **Question:** Which of the following is/are the DDL statements?
* **Options:**
    * Create
    * Drop
    * Alter
    * **All of the Mentioned** (Correct)
* **Solution Description:** All the mentioned commands are part of DDL statements.
