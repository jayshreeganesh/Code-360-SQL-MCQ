It looks like you've successfully completed a quiz on SQL and DBMS fundamentals! Here is the content of those five screenshots converted into a clean, structured Markdown format.

---

## 1. SQL TRUNCATE Command

**Problem Statement:** What does the TRUNCATE command do?

* **Options:**
* Deletes a table
* **Removes all rows** (Correct)
* Modifies table structure
* Sorts data



**Solution Description:**
TRUNCATE removes all rows from a table but retains the table structure.

---

## 2. Drawback of DBMS

**Problem Statement:** What is/are the drawback(s) of using DBMS:

* **Options:**
* Requires training
* Complex to use
* It is costly
* **All of the above** (Correct)



**Solution Description:**
Using DBMS leads to the following disadvantages:

* It requires training because of the complexity associated with it.
* It is complex to use for non-technical users as it isn't easy to maintain and manage the database systems.
* It has a very high cost associated with it as it requires a high-speed processor and huge memory size to use the database on the DBMS.

---

## 3. Entity Subgrouping

**Problem Statement:** The process of designating sub groupings within the entity set is called as ___________.

* **Options:**
* **Specialization** (Correct)
* Division
* Aggregation
* Finalization



**Solution Description:**
With respect to ER Model, specialisation is the procedure to split up the entities into further sub entities on the basis of their functionalities, specialities and features. These sub-designation of entities are distinctive from other entities in the set.

---

## 4. Higher Abstraction

**Problem Statement:** In DBMS, which of the following is a type of abstraction in which entities with relationships come together to form higher level entity?

* **Options:**
* Generalization
* Specialization
* **Aggregation** (Correct)
* None of the above



**Solution Description:**
Aggregation is used when we need to express a relationship among relationships. It is like abstraction through which relationships are treated as higher-level entities. In this multiple entities are considered as a single entity and again this single entity has a relationship with another entity.

---

## 5. Constraint

**Problem Statement:** Which of the following is not a constraint in SQL?

* **Options:**
* Primary Key
* Not Null
* Check
* **Union** (Correct)



**Solution Description:**
A constraint specifies the rules for allowing or restricting what data will be stored in a table. PRIMARY KEY, NOT NULL, and CHECK are constraints that define rules for data insertion. UNION is an operator that combines the results of two or more SELECT queries into a single result set.

---

Would you like me to explain any of these concepts, like the difference between **Truncate** and **Delete**, in more detail?
