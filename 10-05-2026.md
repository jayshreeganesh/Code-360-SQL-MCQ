## Code360 Problem Set - MCQ

---

### **1. SQL DELETE Statement**

**Problem Statement:**
What does the DELETE statement in SQL do?

**Options:**

* [ ] Retrieves data
* [x] **Removes data**
* [ ] Modifies data
* [ ] Creates a table

**Solution description:**
DELETE removes rows from a table.

---

### **2. Relationship 1**

**Problem Statement:**
In DBMS, a many-to-many relationship between two entities usually results in how many tables?

**Options:**

* [ ] ONE
* [ ] TWO
* [x] **THREE**
* [ ] FOUR

**Solution description:**
A many-to-many relationship between two entities usually results in Three tables.

---

### **3. Query Language**

**Problem Statement:**
In DBMS, Relational algebra is:

**Options:**

* [ ] Data Definition Language
* [ ] Meta Language
* [x] **Procedural query language**

**Solution description:**
Relational algebra in DBMS is a procedural query language that uses a set of operations (like selection, projection, union, etc.) to retrieve and manipulate data from relational databases. It serves as the foundation for SQL and provides a formal way of describing how to fetch data based on specified conditions.

---

### **4. Index Ordering**

**Problem Statement:**
In DBMS, when the records of the main table are sorted on the basis of search key which is also the primary key, and so is the order of the index table, it's called:

**Options:**

* [x] **Primary Indexing**
* [ ] Secondary Indexing with key
* [ ] Clustered Indexing
* [ ] Secondary Indexing with non-key

**Solution description:**
When the indexing is done on the basis of the primary key of the data file it is known as the primary indexing. During the primary indexing, the data file is already ordered to support the primary key. Primary Index is an ordered file whose records are of fixed length with two fields. The primary field of the index replicates the primary key of the information entered in an ordered manner, and therefore the second field of the ordered file contains a pointer that points to the data block where a record containing the keys is available.

---

### **5. Procedure Lookup**

**Problem Statement:**
Suppose you need to look out for the procedure containing the word "blog" (it can be anywhere in the name: at the beginning, in the middle or end of the name), which command should be used in SQL:

**Options:**

* [ ] `SHOW PROCEDURE STATUS WHERE db = 'blog';`
* [ ] `SHOW PROCEDURE STATUS LIKE '%blog';`
* [ ] `SHOW PROCEDURE STATUS WHERE db = '%blog%'`
* [x] **`SHOW PROCEDURE STATUS LIKE '%blog%'`**
