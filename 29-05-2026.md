Here is the content from your uploaded screenshots converted into Markdown format, organized in the sequential order of the quiz (1/5 to 5/5).

---

## 1. Data Definition Language (DDL)

**Problem Statement** Which is a DDL command in SQL?

**Options:**

* [ ] SELECT
* [ ] UPDATE
* [ ] DELETE
* [x] CREATE

**Solution description:** `CREATE` is a DDL command used to create tables or databases.

---

## 2. Projection

**Problem Statement** Which clause from the following corresponds to the projection operation of the relational algebra?

**Options:**

* [ ] From
* [x] Select
* [ ] Where
* [ ] None of these

---

## 3. Constraint

**Problem Statement** Which of the following is not a constraint in SQL?

**Options:**

* [ ] Primary Key
* [ ] Not Null
* [ ] Check
* [x] Union

**Solution description:** A constraint specifies the rules for allowing or restricting what data will be stored in a table. `PRIMARY KEY`, `NOT NULL`, and `CHECK` are constraints that define rules for data insertion. `UNION` is an operator that combines the results of two or more `SELECT` queries into a single result set.

---

## 4. Index Ordering

**Problem Statement** In DBMS, when the records of the main table are sorted on the basis of search key which is also the primary key, and so is the order of the index table, it's called :

**Options:**

* [x] Primary Indexing
* [ ] Secondary Indexing with key
* [ ] Clustered Indexing
* [ ] Secondary Indexing with non-key

**Solution description:** When the indexing is done on the basis of the primary key of the data file it is known as the primary indexing. During the primary indexing, the data file is already ordered to support the primary key. Primary Index is an ordered file whose records are of fixed length with two fields. The primary field of the index replicates the primary key of the information entered in an ordered manner, and therefore the second field of the ordered file contains a pointer that points to the data block where a record containing the keys is available.

---

## 5. Hash Index

**Problem Statement** The method of access that uses key transformation is known as ______.

**Options:**

* [ ] Direct
* [x] Hash
* [ ] Random
* [ ] Sequential
