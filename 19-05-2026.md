Here is the content from all five screenshots converted into clear Markdown format, organized by problem.

---

## Problem 1: SQL DISTINCT Keyword

**Problem Statement:** What does DISTINCT do in SQL?

### Options:

* [ ] Returns all rows
* [x] Filters duplicate rows
* [ ] Sorts the result
* [ ] Joins tables

> ### Solution description
> 
> 
> DISTINCT removes duplicate rows in the result set.

---

## Problem 2: Concurrent Access Explanation

**Problem Statement:** What does concurrent access in a DBMS allow?

### Options:

* [ ] Only one user can access the database at a time
* [x] Multiple users can access the database simultaneously without conflicts
* [ ] Data is replicated across multiple databases for access
* [ ] The database can only handle small amounts of data

> ### Solution description
> 
> 
> DBMS supports concurrent data access, allowing multiple users to interact with the database at the same time without interfering with each other's actions.

---

## Problem 3: Subqueries-1

**Problem Statement:** Suppose a user has 3 queries Q1, Q2 and Q3. These queries are written in a way i.e. Q1(Q2(Q3)). Which SQL query will be executed first?

### Options:

* [ ] Q1
* [ ] Q2
* [x] Q3
* [ ] All are updated at the same time

> ### Solution description
> 
> 
> Subqueries is a way of using queries within a particular query. It is accomplished using the parenthesis. The inner query is always executed first, followed by the execution of the outer query. Inner query results in a single value or set of values as an output. In the above example, Q3 is the innermost query, so it should be executed first.

---

## Problem 4: Indexing Structure Basics

**Problem Statement:** In a multi-level index, what does the first-level index typically point to?

### Options:

* [ ] The entries in the second-level index.
* [x] The raw data blocks in the database.
* [ ] The entries in the third-level index.
* [ ] The memory used by the database engine.

> ### Solution description
> 
> 
> The first-level index contains references (pointers) to the blocks of raw data in the database table. These entries enable faster access to the actual data.

---

## Problem 5: Database Schema

**Problem Statement:** In DBMS, database schema is part of which design process:

### Options:

* [ ] Conceptual design
* [x] Logical design
* [ ] Physical Design
* [ ] None of the above

> ### Solution description
> 
> 
> Going by the definition of database schema, it is the skeleton structure that demonstrates the logical view of the complete database. It is responsible for describing the organization of data and how the relations will be associated among them. It also formulates all the conditions that are to be applied on the data.
