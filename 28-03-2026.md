It looks like you've been busy with some DBMS practice! Here is the content of the five screenshots from Code360 by Coding Ninjas, organized in the order you completed them.

---

## 1. SQL UPDATE Statement
**Problem Statement:** What does the SQL UPDATE statement do?

* **Options:**
    * Adds data
    * Deletes data
    * **Modifies data** (Selected)
    * Retrieves data
* **Solution Description:** `UPDATE` modifies existing records in a table.

---

## 2. DBMS Challenges
**Problem Statement:** Which of the following is a disadvantage of DBMS?

* **Options:**
    * Ensures fast data retrieval
    * Reduces complexity in database management
    * **Requires specialized knowledge for setup and maintenance** (Selected)
    * Provides automatic crash recovery
* **Solution Description:** While DBMS provides many advantages, it also requires specialized knowledge and expertise for setup, management, and ongoing maintenance, which can be a challenge for some organizations.

---

## 3. Index Ordering
**Problem Statement:** In DBMS, when the records of the main table are sorted on the basis of search key which is also the primary key, and so is the order of the index table, it's called:

* **Options:**
    * **Primary Indexing** (Selected)
    * Secondary Indexing with key
    * Clustered Indexing
    * Secondary Indexing with non-key
* **Solution Description:** When the indexing is done on the basis of the primary key of the data file it is known as the primary indexing. During the primary indexing, the data file is already ordered to support the primary key. Primary Index is an ordered file whose records are of fixed length with two fields. The primary field of the index replicates the primary key of the information entered in an ordered manner, and therefore the second field of the ordered file contains a pointer that points to the data block where a record containing the keys is available.

---

## 4. 2NF (Normalization)
**Problem Statement:** Consider the schema $R(S, T, U, V)$ and the dependencies $S \rightarrow T, T \rightarrow U, U \rightarrow V, V \rightarrow S$ in DBMS. Let $R = \{R1, R2\}$ such that $R1 \cap R2 = \Phi$. Then the decomposition is:

* **Options:**
    * not in 2NF
    * in 2NF but not in 3NF
    * in 3NF but not in 2NF
    * **in both 2NF and 3NF** (Selected)
* **Solution Description:** Then, the decomposition is in 2NF but not in 3NF.
> **Note:** There appears to be a discrepancy in the screenshot between the selected answer ("in both 2NF and 3NF") and the text in the "Solution description" ("is in 2NF but not in 3NF").

---

## 5. Hashed File
**Problem Statement:** The physical location of a record is determined by a mathematical formula that transforms a file key into a record location is:

* **Options:**
    * B-Tree File
    * **Hashed File** (Selected)
    * Indexed File
    * Sequential file

---

Great job on hitting 5/5! Would you like me to explain the logic behind that normalization problem (the 2NF/3NF one) since the solution text and the selected answer seem a bit at odds?
