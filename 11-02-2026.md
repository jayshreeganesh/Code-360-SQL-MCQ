Here is the content from your screenshots converted into organized Markdown format.

---

## 1. SQL DISTINCT Keyword

**Problem Statement:** What does `DISTINCT` do in SQL?

* **Options:**
* Returns all rows
* **Filters duplicate rows** (Correct)
* Sorts the result
* Joins tables


* **Solution Description:** `DISTINCT` removes duplicate rows in the result set.

---

## 2. Clustering Drawbacks

**Problem Statement:** Which of the following is **not** an advantage of database clustering?

* **Options:**
* Load Balancing
* Data Redundancy
* Increased Availability
* **Decreased Availability** (Correct)


* **Solution Description:** 1.  **Data Redundancy:** Clustering stores the same data across multiple servers via synchronization.
2.  **Load Balancing:** Distributes incoming requests across multiple servers to prevent overload.
3.  **Increased Availability:** If one server fails, others remain available to handle requests, ensuring high availability.

---

## 3. Subqueries (Operators)-1

**Problem Statement:** The given below relation schema is used to store information about the employees of a company, where **empid** is the primary key and **depttid** indicates the department of the employee.
`Emp(empid, name, gender, salary, depttid)`

Guess the output of the following Query:

```sql
SELECT name, MIN(salary) 
FROM Emp 
GROUP BY depttid 
HAVING MIN(salary) > (SELECT min(salary) FROM employees WHERE depttid = 20);

```

* **Options:**
* **It gives the names and minimum salary greater than department 20 of all employees.** (Correct)
* It gives the name and salaries of the employees in department 20.
* It gives the names and minimum salaries of all the employees.
* It throws an error.


* **Solution Description:** It will execute successfully. The output table will have two columns: `name` and the minimum salary of every department that has a minimum salary greater than the minimum salary of department 20.

---

## 4. Deadlock in Databases

**Problem Statement:** What is a deadlock in the context of database transactions?

* **Options:**
* A situation where a transaction is terminated due to insufficient resources.
* **A state where multiple transactions are waiting for each other to release resources, causing a circular wait.** (Correct)
* A condition where a transaction fails to acquire a lock due to high contention.
* A scenario where transactions complete successfully but with inconsistent data.


* **Solution Description:** A deadlock occurs when two or more transactions hold resources and simultaneously request resources held by each other, creating a circular wait that prevents any transaction from proceeding.

---

## 5. Relational Algebra

**Problem Statement:** In the context of DBMS, relational algebra is:

* **Options:**
* Non procedural query language
* High level language
* **Procedural query language** (Correct)
* None of the above


* **Solution Description:** Relational Algebra is a procedural query language in which the user specifies the set of operations to be performed sequentially to get the desired output.

---

**Would you like me to explain the difference between Procedural (Relational Algebra) and Non-Procedural (SQL) languages in more detail?**
