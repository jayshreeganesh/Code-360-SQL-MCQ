Here is the content from the uploaded screenshots converted into a clear, scannable Markdown format.

---

## 1. SQL INNER JOIN

**Problem Statement:** What does an INNER JOIN return in SQL?

* **Options:**
* All records from both tables
* **Only matching records** (Correct)
* Only unmatched records
* First table records


* **Solution Description:** INNER JOIN returns rows when there is a match in both tables.

---

## 2. Foreign Key

**Problem Statement:** Identify the Foreign Key from table “Sales” in Database.

**Table Client:**

* ClientID (Primary Key)
* clientname
* clientphone

**Table Sales:**

* SalesID (Primary Key)
* ClientID
* Profit
* **Options:**
* SalesID
* Profit
* Clientphone
* **ClientID** (Correct)



---

## 3. Null values

**Problem Statement:** In DBMS, which of the following aggregate functions does not ignore null values?

* **Options:**
* Max(attribute)
* Count(attribute)
* Min(attribute)
* **Count(*)** (Correct)
* Both ii) and iv)
* Both i) and iii)


* **Solution Description:** `*` selects all values including null, thus, `count(*)` considers null values also.

---

## 4. Two phase lock

**Problem Statement:** In DBMS, the two-phase locking protocol consists of which of the following phases?

* **Options:**
* Growing phase
* Shrinking phase
* **More than one of the mentioned** (Correct)
* None of the mentioned


* **Solution Description:** The two-phased locking protocol consists of the growing phase and the shrinking phase.

---

## 5. Non Dense Index

**Problem Statement:** In DBMS, an example of a non-dense index is ________.

* **Options:**
* Ternary
* Secondary
* Primary
* **Clustering** (Correct)



---

Would you like me to explain the logic behind any of these database concepts, such as why `Count(*)` behaves differently than `Count(column)`?
