Here is the content from the uploaded screenshots converted into clear Markdown format, organized by the specific topic of each MCQ.

---

## 1. SQL LIKE Operator

* **Problem Statement:** What is the purpose of LIKE in SQL?
* **Options:**
* [ ] Compare exact values
* [x] Pattern matching
* [ ] Sort data
* [ ] Group data


* **Solution Description:** LIKE is used for pattern matching with wildcards.

---

## 2. Aggregate / Subquery

* **Problem Statement:** Find all the tuples having a temperature greater than 'Paris' in SQL.
* **Options:**
* [x] `SELECT * FROM weather WHERE temperature > (SELECT temperature FROM weather WHERE city = 'Paris')`
* [ ] `SELECT * FROM weather WHERE temperature > (SELECT * FROM weather WHERE city = 'Paris')`
* [ ] `SELECT * FROM weather WHERE temperature > (SELECT city FROM weather WHERE city = 'Paris')`
* [ ] `SELECT * FROM weather WHERE temperature > 'Paris' temperature`


* **Solution Description:** Subquery—also referred to as an inner query or inner select—is a SELECT statement embedded within a data manipulation language (DML) statement or nested within another subquery.

---

## 3. Transaction Control

* **Problem Statement:** Suppose we have created a savepoint named Spt1, which of the following commands in SQL can be used to rollback the transactions executed after creating the savepoint:
* **Options:**
* [ ] RELEASE SAVEPOINT Spt1
* [ ] COMMIT Spt1
* [x] ROLLBACK TO Spt1
* [ ] ROLLBACK


* **Solution Description:** ROLLBACK transaction - This TCL commands undo all the changes or restore the current state to any previously saved state. General form: ROLLBACK: This rollback the previously committed command to its initial state ROLLBACK TO savepoint: This undoes the current state and restores it to the savepoint

---

## 4. Locking Protocol

* **Problem Statement:** In DBMS, the protocol that indicates when a transaction may lock and unlock each of the data items are called as ______.
* **Options:**
* [x] Locking protocol
* [ ] Unlocking protocol
* [ ] Granting protocol
* [ ] Conflict protocol


* **Solution Description:** The protocol that indicates when a transaction may lock and unlock each of the data items is called as locking protocol. Locking protocols restrict the number of schedules.

---

## 5. Schedules

* **Problem Statement:** In DBMS, the execution sequences in concurrency control are termed as ______.
* **Options:**
* [ ] Serials
* [x] Schedules
* [ ] Organizations
* [ ] Timetables


* **Solution Description:** The execution sequences in concurrency control are known as schedules.
