It looks like you've been busy with some database and software architecture quizzes! Here is the content of those five screenshots converted into a clean Markdown format.

---

## 1. SQL DEFAULT Constraint

* **Problem Statement:** What does the DEFAULT constraint do?
* **Selected Option:** **Sets a default value** (Correct)
* **Other Options:**
* Ensures unique values
* Checks within a range
* Prevents NULL values


* **Solution Description:** `DEFAULT` sets a default value for a column when no value is specified.

---

## 2. Result Computation

* **Problem Statement:** In a software architecture which of the following is responsible for computing the result and returning it:
* **Selected Option:** **Server** (Correct)
* **Other Options:**
* Client


* **Solution Description:** In software architecture Server is responsible for all the heavy computations and returning their result and client is only responsible for displaying them through an interface.

---

## 3. Commands DDL

* **Problem Statement:** SQL has how many main commands for DDL:
* **Selected Option:** **3** (Correct)
* **Other Options:**
* 1
* 2
* 4


* **Solution Description:** Create, Delete, Alter these are 3 main command [sic].

---

## 4. Data Modelling

* **Problem Statement:** Data modelling helps in achieving abstraction in DBMS.
* **Selected Option:** **True** (Correct)
* **Other Options:**
* False


* **Solution Description:** Abstraction means showing only what is required and hiding the extra information. This is achieved through the three kind of data modelling: Conceptual, Representational and Physical data models.

---

## 5. Dirty Read Example

* **Problem Statement:** Which of the following is an example of a dirty read in database transactions?
* **Selected Option:** **A transaction reads data that another transaction has not yet committed.** (Correct)
* **Other Options:**
* A transaction updates the same record as another transaction, overwriting changes.
* A transaction retrieves inconsistent results due to other transactions inserting or deleting rows.
* A transaction locks a table, preventing others from accessing it.


* **Solution Description:** A dirty read occurs when one transaction reads uncommitted changes made by another transaction. If the changes are later rolled back, the data read becomes invalid.

---

Great job on getting a perfect 5/5 on the quiz! Would you like me to explain any of these concepts, like the difference between DDL and DML, in more detail?
