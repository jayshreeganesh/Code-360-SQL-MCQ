Here is the content from your screenshots, organized by the problem titles and converted into a clean Markdown format.

---

## 1. SQL Constraints

**Problem Statement:** What does a UNIQUE constraint enforce in SQL?

* **Options:**
* NULL values
* **Unique values (Correct)**
* Default values
* Grouping rows



**Solution Description:** UNIQUE constraint ensures all values in a column are different.

---

## 2. Semester Update

**Problem Statement:** Which of the following is the most efficient way in SQL to update the current value of "Semester" attribute by 1, for all the rows of the table 'Student':

* **Options:**
* Write 'n' update commands, if 'n' number of rows are present in the table.
* Instead of updating, insert all the records again in the table.
* **Update Student SET Semester = Semester + 1; (Correct)**
* We cannot update all the rows at once.



**Solution Description:** We definitely can update all rows at once. This is possible as we have not used any WHERE condition to limit the rows on which we would like to perform UPDATE operation. For example in this case for all the rows of the Student table, the Semester column would get updated by 1.

---

## 3. Replace vs Update

**Problem Statement:** Choose the correct statement regarding replace and update in database:

* **Options:**
* If the row which we want to replace is not present, replace will not do anything but update will add a new row.
* Update delete the old data and then add new data while replace always makes changes in current data.
* **If the row is not present, replace will add a new row and update will not do anything. (Correct)**
* Replace is faster than update because it always adds new data instead of making changes in existing one.



**Solution Description:** REPLACE adds new rows if they don't exist and updates old rows in case they already existed. UPDATE make changes in existing rows. Hence, A is not correct as Replace will insert the row and update will not do anything if row is not present. B is not correct as Update makes changes in existing data while replace makes changes according to row presence or absence. D is not correct as Update is faster because replace have to add new data while update have to make changes in current one.

---

## 4. Relational Algebra

**Problem Statement:** In DBMS, Relational Algebra does not have

* **Options:**
* Selection operator
* Projection operator
* **Aggregation operator (Correct)**
* Division operator



**Solution Description:** Relational Algebra does not have an aggregator operator because it was designed as a set-based, foundational query language focused on basic operations like selection, projection, union, difference, and Cartesian product. Aggregation functions like SUM, COUNT, and AVERAGE are more complex operations that were introduced in SQL to meet practical needs, but they extend beyond the original minimalistic mathematical framework of Relational Algebra.

---

## 5. Address field

**Problem Statement:** In DBMS, the address field of a person should not be part of the primary key, since it is likely to?

* **Options:**
* Dependent
* Too long
* **Changed (Correct)**
* Not changed



**Solution Description:** Address field of a person should not be part of primary key, since it is likely to Changed [sic].

---

Would you like me to explain any of these database concepts, such as why **Primary Keys** should be immutable?
