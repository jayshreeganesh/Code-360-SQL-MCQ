Based on the screenshots provided, here is the content of the five SQL and DBMS questions formatted in Markdown.

---

## 1. SQL ORDER BY Clause

**Problem Statement:** What is the purpose of `ORDER BY`?

**Options:**

* Join tables
* Filter data
* Group data
* **Sort data** (Correct)

**Solution Description:** `ORDER BY` sorts the result set in ascending or descending order.

---

## 2. Union of primary key

**Problem Statement:** The union of primary keys of the related entity sets becomes a ------ of the relation.

**Options:**

* **Super Key** (Correct)
* Candidate key
* Foreign key
* Primary key

---

## 3. Handling Large Indexes

**Problem Statement:** What happens when the first-level index in a database becomes too large?

**Options:**

* The system stops using indexes for searches.
* **A second-level index is created to point to entries in the first-level index.** (Correct)
* The first-level index is divided into smaller tables.
* The database automatically optimizes queries without using the index.

**Solution Description:** When the first-level index becomes too large to fit in memory, a second-level index is created. This index points to entries in the first-level index, enabling efficient management of large datasets.

---

## 4. Delete & Truncate

**Problem Statement:** Which of the following statements is true in SQL?

**Options:**

* **DELETE does not free the space containing the table and TRUNCATE free the space containing the table** (Correct)
* Both DELETE and TRUNCATE free the space containing the table
* Both DELETE and TRUNCATE does not free the space containing the table
* DELETE free the space containing the table and TRUNCATE does not free the space containing the table

**Solution Description:** The SQL `TRUNCATE` command is used to delete all the rows from the table and free the space containing the table.

---

## 5. DBMS Architecture

**Problem Statement:** In the context of DBMS, if your laptop has an application like MS Access that is fetching data from another system, it is an example of which type of architecture?

**Options:**

* 1 tier
* **2 tier** (Correct)
* 3 tier
* None of the above.

**Solution Description:** Since in a two-tier architecture, an interface runs on a client, and a data layer gets stored on a server. In the case of MS access also, an interface is running on your laptop which is actually fetching data from some database on another system. Hence it is an example of 2-tier architecture.

---

Would you like me to explain any of these concepts, such as the difference between **Super Keys** and **Candidate Keys**, in more detail?
