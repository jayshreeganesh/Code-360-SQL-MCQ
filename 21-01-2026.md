It looks like you've successfully completed a quiz on SQL and Database fundamentals! Here is the content of your screenshots converted into organized Markdown format.

---

## **Quiz Summary: SQL & Database Fundamentals**

### **1. SQL BETWEEN Operator**

* **Question:** What does the `BETWEEN` operator do in SQL?
* **Correct Answer:** **Checks within a range**
* **Solution Description:** `BETWEEN` checks if a value lies within a specified range.

---

### **2. Hashing Techniques Comparison**

* **Question:** What is an advantage of dynamic hashing over static hashing?
* **Correct Answer:** **It avoids overflow buckets by adjusting the number of buckets dynamically.**
* **Solution Description:** Dynamic hashing expands or contracts the number of buckets as needed, redistributing data to avoid overflow issues. This makes it more efficient for systems with unpredictable or growing datasets.

---

### **3. SQL Database Structure**

* **Question:** SQL uses ________ format to store data.
* **Correct Answer:** **tables**
* **Solution Description:** SQL is a structured, relational database that uses tables to store the data. It has rows and columns and uses a tabular format.

---

### **4. Database Language Subsets**

* **Question:** Which is the subset of SQL commands used to manipulate Oracle Database structures, including tables?
* **Correct Answer:** **Data Definition Language (DDL)**
* **Solution Description:** The DDL is used to manage table and index structure. `CREATE`, `ALTER`, `RENAME`, `DROP`, and `TRUNCATE` statements are the names of few data definition elements.

---

### **5. Create User Command**

* **Question:** Which command creates a new database user?
* **Correct Answer:** **CREATE USER**
* **Solution Description:** In many SQL databases, `CREATE USER` is used to introduce a new user account with specific privileges.

---

### **Results Overview**

* **Score:** 5/5 done
* **Status:** Great job on completing the quiz!
* **Total EXP:** 32,500

Would you like me to explain the difference between **DDL** (Data Definition) and **DML** (Data Manipulation) in more detail?
