It looks like you've been busy crushing some SQL quizzes! Here is the content from your five screenshots organized into clear Markdown format for your notes.

---

## 1. SQL WHERE Clause

* **Question:** What is the purpose of the `WHERE` clause in SQL?
* **Correct Answer:** **Filter data**
* **Solution Description:** `WHERE` is used to filter records in a query.

---

## 2. Exclusive Mode

* **Question:** In DBMS, if a transaction has obtained a ________ lock, it can both read and write on the item.
* **Correct Answer:** **Exclusive mode**
* **Solution Description:** If a transaction has obtained an exclusive mode lock, then it can both read and write on the item on which it is operating.

---

## 3. Key Update

* **Scenario:** There are two tables: `Employee` and `Payment`. The `id` of the `Employee` table acts as a foreign key for the `Payment` table. It has been realized that the current `id` (103) of an employee should be replaced with his correct `id`, which is 100.
* **Question:** Which of the following statements is correct for this scenario?
* **Correct Answer:** **We should make sure that if we are updating the correct value of id in the Employee table, it should also get updated in another table referencing it (Payment table) so that the data remains consistent.**
* **Solution Description:** We can update the current `id` of employees from 103 to 100... We should update the correct value of `current_id` in the employee as well as the payment table... so that the data remains consistent.

---

## 4. Delete & Truncate

* **Question:** Which of the following statements is true in SQL?
* **Correct Answer:** **DELETE does not free the space containing the table and TRUNCATE free the space containing the table**
* **Solution Description:** The SQL `TRUNCATE` command is used to delete all the rows from the table and free the space containing the table.

---

## 5. TCL Command

* **Question:** Which of the following are TCL commands in DBMS?
* **Correct Answer:** **ROLLBACK and SAVEPOINT**
* **Solution Description:** Transaction control commands manage changes made by DML commands. These SQL commands are used for managing changes affecting the data.

---

Great job on getting a **5/5 score** on the quiz! Would you like me to explain the difference between DDL, DML, and TCL commands in more detail to help with your studies?
