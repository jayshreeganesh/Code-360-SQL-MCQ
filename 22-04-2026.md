Based on the screenshots provided, here is the content of the five DBMS-related multiple-choice questions from Code360 formatted in Markdown.

---

## 1. SQL INNER JOIN
**Problem Statement:** What does an INNER JOIN return in SQL?

**Options:**
* All records from both tables
* **Only matching records** (Correct)
* Only unmatched records
* First table records

**Solution Description:** INNER JOIN returns rows when there is a match in both tables.

---

## 2. Normalization Step
**Problem Statement:** Katherine was the assistant to the DBMS professor at her college. She was given a simple task of converting composite attributes into individual attributes for a table. What normalization is she performing in context to DBMS?

**Options:**
* **First** (Correct)
* Second
* Third
* None of the above

**Solution Description:** First Normal Form (1NF) is the first step of the normalization process. For a relation to justify 1NF, it needs to satisfy 4 basic conditions: 
1. Each attribute should contain atomic values (i.e., no multivalued attributes).
2. Each value stored in an attribute should be of the same type.
3. All the attributes in a table should have unique names.
4. The order of the data stored in the table doesn't matter. 

In the given question, Katherine was given the task of converting composite attributes to individual attributes for a table. So it is in the First normal form.

---

## 3. Clustered Index
**Problem Statement:** Which one is true about the clustered index?

**Options:**
* Not linked with table
* **Built on unique key by default** (Correct)
* Not built on unique key
* None of the mentioned

**Solution Description:** A clustered index is built by default on unique key columns. Nonclustered indexes have a structure separate from the data rows. A nonclustered index contains the nonclustered index key values and each key-value entry has a pointer to the data row that contains the key value.

---

## 4. Result Computation
**Problem Statement:** In a software architecture which of the following is responsible for computing the result and returning it:

**Options:**
* Client
* **Server** (Correct)

**Solution Description:** In software architecture Server is responsible for all the heavy computations and returning their result and client is only responsible for displaying them through an interface.

---

## 5. Recursive Relation
**Problem Statement:** In DBMS, a recursive relationship is a relationship between an entity and __________.

**Options:**
* **itself** (Correct)
* a subtype entity
* an archetype entity
* an instance entity

**Solution Description:** In DBMS, a recursive relationship is a relationship between an entity and itself. This occurs when an entity is related to itself in a relationship, such as an employee supervising another employee within the same entity set.
